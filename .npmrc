auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
# Hoist Prisma to the workspace root so the root-level schema and the api
# share one generated client instance (avoids ERR auto-install on generate).
public-hoist-pattern[]=*prisma*
